/** * File not found controller *  * @module 404notfound */'use strict';var fs = require('fs'),	template = require('nodework').template,    globalModel = require('../models/global'),	logger = require('nodework').logger,	config = require('nodework').config;/** * Builds html for 404 error *  * @param {Object} callback Returns the 404 html */exports.build = function(callback) {    var modelData = {};        modelData.siteDetails = globalModel.getDetails();	template.getContents(modelData, '404notfound', config.defaultLayout, function(data) {		callback(data);	});	};