/** * File not found controller *  * @module 404notfound */'use strict';var fs = require('fs'),	template = require('../application/template'),    baseModel = require('../models/base'),	logger = require('../application/logger');/** * Builds html for 404 error *  * @param {Object} callback Returns the 404 html */exports.build = function(callback) {    var modelData = {};        modelData.siteDetails = baseModel.getDetails();	template.getContents(modelData, '404notfound', function(data) {		callback(data);	});	};